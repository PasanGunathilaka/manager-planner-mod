# Verification Report: progress-notes-and-promise-tracking

**Verified:** 2026-08-03
**Model:** Claude Sonnet 5
**Verdict:** PASS

## Acceptance Criteria

- ✅ **AC1** — `dotnet build` at the solution root succeeds with 0 errors — Independently re-ran `dotnet build` against the working tree; output: `Build succeeded. 0 Warning(s) 0 Error(s)`, matching the payload's captured build output verbatim.

- ✅ **AC2** — Submitting the add-note form with non-empty text persists exactly one new `ProgressNote` row with `Text` trimmed, `WorkItemId` set to the row's task, `AuthorId` via `GetCurrentManagerIdAsync()`, `NoteDate` equal to the picked note date, `MeetingId` equal to the selected meeting (or null) — `TaskRow.razor.AddNoteAsync`: `var authorId = await PlanningService.GetCurrentManagerIdAsync();` then `await PlanningService.AddNoteAsync(WorkItem.Id, _newNoteText, authorId, _newNoteMeetingId, _newNoteIsPromise, _newNoteIsPromise ? _newNotePromisedDate : null, _newNoteDate);`. `PlanningService.AddNoteAsync`: `var note = new ProgressNote { WorkItemId = taskId, Text = text.Trim(), AuthorId = authorId, MeetingId = meetingId, ... NoteDate = effectiveDate }; db.ProgressNotes.Add(note); await db.SaveChangesAsync();` — a single `Add`+`SaveChangesAsync` call, one row. The Meeting dropdown's "— No meeting —" option is `<MudSelectItem Value="@((int?)null)">` bound to `_newNoteMeetingId`, so unselected → `MeetingId = null`.

- ✅ **AC3** — Unchecked "is a promise" checkbox persists `IsPromise = false` and `PromisedDate = null` even if a date remains picked — Call site: `_newNoteIsPromise ? _newNotePromisedDate : null` passed as the `promisedDate` argument, and `_newNoteIsPromise` (false) passed as `isPromise`. Service: `IsPromise = isPromise, PromisedDate = promisedDate` — with `isPromise=false` this always yields `IsPromise=false, PromisedDate=null` regardless of the picker's retained value. Confirmed this reproduces both real legacy view models exactly: `ExecutivePlanning.Desktop/ViewModels/MainWindowViewModel.cs:188`: `NewNoteIsPromise ? NewNotePromisedDate?.UtcDateTime : null` and `ManagerPlanner.Desktop/ViewModels/MainViewModel.cs:164`: `promisedDate: NewNoteIsPromise ? NewNotePromisedDate?.UtcDateTime : null`.

- ✅ **AC4** — Checked checkbox persists `IsPromise = true` and `PromisedDate` equal to the picked promised date — Same ternary: when `_newNoteIsPromise` is true, `_newNotePromisedDate` is passed through unchanged and stored as `PromisedDate = promisedDate`.

- ✅ **AC5** — Empty/whitespace-only text shows exact message `"The note is empty — type what was said before saving."`, creates no row, no unhandled exception — `PlanningRules.ValidateNoteText`: `if (t.Length == 0) { throw new ValidationException("The note is empty — type what was said before saving."); }`, called before the DB context is even opened in `AddNoteAsync` (`await using var db = ...` comes after both `ValidateNoteText`/`ValidateNoteDate` calls), so no row is ever created on this path. `TaskRow.razor.AddNoteAsync` wraps the call in `catch (ManagerPlanner.Core.Validation.ValidationException ex) { _noteErrorMessage = ex.Message; }` — caught, not unhandled. Cross-checked against the captured golden-master fixture `.specclaw/baseline/fixtures/GM-005.json`, which records the real legacy app throwing the identical message text for whitespace input: `"Message": "The note is empty — type what was said before saving."`.

- ✅ **AC6** — Overlong text (>2000 chars) shows exact message `"The note is too long. Keep it under 2000 characters."`, no row created; exactly 2000 chars accepted — `PlanningRules.cs`: `if (t.Length > MaxNoteText) { throw new ValidationException($"The note is too long. Keep it under {MaxNoteText} characters."); }` with `MaxNoteText = 2000`, so `2000 > 2000` is false (accepted) and `2001 > 2000` is true (rejected). `GM-005.json`'s captured legacy output for `over2001Length: 2001` is `"Message": "The note is too long. Keep it under 2000 characters."` (exact string match) and for `exact2000Length: 2000` is `"Threw": false` — both boundaries match the rebuild's code exactly. Also confirmed the diff corrected the prior paraphrase `"The note is too long — it cannot exceed {MaxNoteText} characters."` → the verbatim text, per `git diff 55cc0a0 6f5eb30 -- src/ManagerPlanner.Core/Validation/PlanningRules.cs`.

- ✅ **AC7** — Note dated more than one month before today shows exact message with correct `earliest` date; exactly one month back accepted — `PlanningRules.ValidateNoteDate`: `var earliestAllowed = today.AddMonths(-NoteBackdateMonths); if (d < earliestAllowed) { throw new ValidationException($"That date is more than a month back. Notes can only be dated on or after {earliestAllowed:MMM dd, yyyy}."); }` — `d == earliestAllowed` is not `<`, so accepted; one day earlier is rejected. `GM-006.json` captured legacy output for `oneDayEarlier` (2026-06-29, one day before the 2026-06-30 boundary computed from anchor date 2026-07-30): `"Message": "That date is more than a month back. Notes can only be dated on or after Jun 30, 2026."` — exact text and date-format match (`MMM dd, yyyy` → "Jun 30, 2026"); `atEarliest` (2026-06-30 itself): `"Threw": false`.

- ✅ **AC8** — Note dated after today shows exact message `"A note cannot be dated in the future."`; today accepted — `if (d > today) { throw new ValidationException("A note cannot be dated in the future."); }` — today is not `>` today (accepted), tomorrow is (rejected). `GM-007.json`: `"today": {"Threw": false}`, `"tomorrow": {"Threw": true, "Message": "A note cannot be dated in the future."}` — exact match. Diff confirms the prior paraphrase `"The note date cannot be in the future."` was corrected to this verbatim text.

- ✅ **AC9** — Note history list shows every note for the task, ordered by `NoteDate` descending (newest first) — `PlanningService.GetNotesForTaskAsync`: `.Where(n => n.WorkItemId == taskId).OrderByDescending(n => n.NoteDate).ToListAsync();` (no additional filtering), and `TaskRow.razor` renders `@foreach (var note in _notes)` in that returned order with no client-side re-sort.

- ✅ **AC10** — Each row shows the linked meeting's title when set, a placeholder when null; shows a promise indicator (with promised date) when `IsPromise` true, none when false — `@note.NoteDate.ToString("yyyy-MM-dd") — @note.Author.FullName — @(note.Meeting?.Title ?? "—")` — em-dash placeholder, not blank, when `Meeting` is null (supported by `.Include(n => n.Meeting)` in `GetNotesForTaskAsync` so `note.Meeting` is populated, not lazy-null). `@if (note.IsPromise) { <MudText ...>Promise due @note.PromisedDate?.ToString("yyyy-MM-dd")</MudText> }` — no element rendered at all when `IsPromise` is false (no empty placeholder row either).

- ✅ **AC11** — No `PlanningService` method exists beyond the thirteen existing ones plus `AddNoteAsync`/`GetNotesForTaskAsync` (fifteen total) — Read `src/ManagerPlanner.Core/Services/PlanningService.cs` in full (253 lines): counted exactly 15 public methods (`GetProjectsAsync`, `AddProjectAsync`, `GetProjectSummaryAsync`, `GetCurrentManagerIdAsync`, `AddObjectiveAsync`, `GetPlannerForProjectAsync`, `AddTaskAsync`, `GetTeamMembersAsync`, `GetUngroupedTasksForProjectAsync`, `ChangeStatusAsync`, `ToggleChecklistItemAsync`, `GetMeetingsForProjectAsync`, `AddMeetingAsync`, `AddNoteAsync`, `GetNotesForTaskAsync`). `git diff 55cc0a0 6f5eb30 -- src/ManagerPlanner.Core/Services/PlanningService.cs` shows only an appended block adding exactly `AddNoteAsync` and `GetNotesForTaskAsync` — no other method was touched or added.

- ✅ **AC12** — No UI control edits/deletes an existing `ProgressNote`; no Accountability/`Verdict` view exists anywhere in the diff — Full diff (`git diff 55cc0a0 6f5eb30 -- src/`) touches exactly `PlanningService.cs` (+35/-0, additive only), `PlanningRules.cs` (message-string edits only, +3/-3), `ProjectDetail.razor` (+2/-2, only adding the `Meetings="_meetings"` parameter to two existing `<TaskRow>` tags), and `TaskRow.razor` (+101, additive Notes section/form). No `Verdict`, `Accountability`, delete, or edit control appears anywhere in this diff — the only note-related control added is the "Add note" `EditForm`.
  - ⚠️ Edge case (documented, not a defect): two notes sharing the identical `NoteDate` have no secondary sort key (`OrderByDescending(n => n.NoteDate)` alone) — this reproduces the legacy gap deliberately per spec's Edge Cases section, consistent with the same shape already accepted elsewhere in this codebase (`GetProjectsAsync`/`GetMeetingsForProjectAsync`).

## Test Results

No tests configured. `.specclaw/config.yaml` sets `test_command: ""` and `test_policy: "none"`; no test project exists anywhere in `manager-planner-mod` (confirmed via file search — only `.specclaw/baseline/harness/TestDb.cs` and `.specclaw/parity-harness/TestDb.cs` exist, both unrelated harness/parity tooling, not a project test suite). This is consistent with the project's established policy for this repo and is not a gate failure.

## Issues Found

No issues found.

Independent notes on the prior live-verification claim referenced in this task: the build's `status.md` states a coding agent drove the running app via `claude-in-chrome` and cross-checked persisted SQLite rows for AC2–AC12, including exact validation message text at the empty/2001/2000-char and one-month-back/tomorrow date boundaries, reporting all as passing. This independent, code-reading-only pass — cross-checked additionally against the real legacy source at `../manager-planner/src/ExecutivePlanning.Core/Services/{PlanningService.cs,PlanningValidation.cs}` and the two legacy view models' `AddNoteAsync` call sites, plus the captured golden-master fixtures `GM-005.json`/`GM-006.json`/`GM-007.json` — agrees with that prior claim in full. No discrepancy was found between the prior live-verification claim and this static re-check.

## Summary

**Passed:** 12/12 criteria
**Failed:** 0/12 criteria
**Verdict:** PASS
